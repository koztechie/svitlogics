// src/components/layout/CookieConsentManager.server.tsx
import React from "react";

// Ця версія компонента буде використовуватися ЛИШЕ на сервері.
// Вона не робить абсолютно нічого.
const CookieConsentManagerServer: React.FC = () => {
  return null; // Рендеримо пустку
};

export default CookieConsentManagerServer;
